export const metadata = {
  title: "Projects | Yashaswini Mullalli",
  description: "Data analysis and engineering projects by Yashaswini Mullalli.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
